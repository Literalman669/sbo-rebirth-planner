/**
 * Optional SpacetimeDB cloud sync for planner data.
 *
 * 1) Publish the module (see ../spacetimedb-module/README.md).
 * 2) Uncomment and set your published database name:
 *
 * window.SBO_STDB_CONFIG = {
 *   uri: "https://maincloud.spacetimedb.com",
 *   databaseName: "your-unique-database-name",
 * };
 */
